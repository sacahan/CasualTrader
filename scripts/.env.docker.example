# ==================== Docker 部署環境配置示例 ====================
# 複製此文件為 .env.docker 並根據實際需求修改
# 用法：docker-compose --env-file .env.docker up -d

# ==================== OpenAI API ====================
OPENAI_API_KEY=your-openai-api-key-here

# ==================== GitHub Integration ====================
GITHUB_PERSONAL_ACCESS_TOKEN=your-github-token-here

# ==================== Perplexity API ====================
PERPLEXITY_API_KEY=your-perplexity-api-key-here

# ==================== MCP 設定 ====================
# CASUAL_MARKET_SSE_URL: casual-market-mcp 的 SSE 連接 URL
CASUAL_MARKET_SSE_URL=http://sacahan-ubunto:8066/sse

# ==================== CORS 設定 ====================
CORS_ORIGINS=["http://localhost:3000", "http://localhost:5173", "http://localhost:8000"]
CORS_ALLOW_CREDENTIALS=true

# ==================== PostgreSQL 設定 ====================
POSTGRES_USER=casualtrader
POSTGRES_PASSWORD=casualtrader123
POSTGRES_DB=casualtrader
POSTGRES_PORT=5432

# ==================== API 設定 ====================
API_HOST=0.0.0.0
API_PORT=8000

# ==================== 日誌設定 ====================
LOG_LEVEL=INFO
LOG_ROTATION=500 MB
LOG_RETENTION=30 days
LOG_COMPRESSION=zip

# ==================== 資料庫連接設定 ====================
# Docker 內部應使用服務名稱 "postgres" 而非 localhost
DATABASE_URL=postgresql+asyncpg://{user}:{password}@postgres:5432/{database}
DATABASE_ECHO=false
DATABASE_POOL_SIZE=5
DATABASE_MAX_OVERFLOW=10
DATABASE_POOL_TIMEOUT=30
DATABASE_POOL_RECYCLE=3600

# ==================== Agent 設定 ====================
MAX_AGENTS=10
DEFAULT_AI_MODEL=gpt-4o-mini
DEFAULT_INITIAL_CAPITAL=10000000
DEFAULT_MAX_TURNS=30
DEFAULT_AGENT_TIMEOUT=300
DEFAULT_MODEL_TEMPERATURE=0.7
SKIP_MARKET_CHECK=false
SKIP_AGENT_GRAPH=true

# ==================== WebSocket 設定 ====================
WS_HEARTBEAT_INTERVAL=30
WS_MAX_CONNECTIONS=100

# ==================== 環境設定 ====================
ENVIRONMENT=production
DEBUG=false
