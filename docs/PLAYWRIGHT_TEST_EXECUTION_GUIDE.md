# 🎭 Playwright E2E 測試執行指南

**日期**: 2025-11-12
**目標**: Phase 3.2.4 - 驗證 RiskMetricsCard 完整功能
**預計時間**: ~90 分鐘

---

## 📋 前置檢查清單

請在開始測試前確認以下事項：

- [ ] **Node.js 環境**

  ```bash
  node --version  # 應為 v18+
  ```

- [ ] **依賴已安裝**

  ```bash
  cd frontend && npm list @playwright/test
  ```

- [ ] **後端服務可用** (8000)
  - API 端點正確響應
  - 性能數據可正常返回

- [ ] **測試文件存在**

  ```bash
  ls -la frontend/tests/e2e/E2E-003-risk-metrics.spec.js
  ```

---

## 🚀 快速啟動 (5 分鐘)

### 步驟 1: 安裝依賴

```bash
cd /Users/sacahan/Documents/workspace/CasualTrader/frontend

# 安裝/更新 npm 包
npm install

# 驗證 Playwright 已安裝
npm list @playwright/test
```

**預期輸出**:

```
casualtrader-frontend@0.1.0 /Users/sacahan/.../frontend
└── @playwright/test@1.56.1
```

### 步驟 2: 準備測試環境

**終端 1 - 啟動後端服務**:

```bash
cd /Users/sacahan/Documents/workspace/CasualTrader/backend

# 方法 A: 使用 run_server.py
python run_server.py

# 或方法 B: 使用 uvicorn 直接啟動
uvicorn src.api.server:app --host 0.0.0.0 --port 8000 --reload
```

**預期輸出**:

```
INFO:     Uvicorn running on http://0.0.0.0:8000
INFO:     Application startup complete
```

**終端 2 - 啟動前端服務**:

```bash
cd /Users/sacahan/Documents/workspace/CasualTrader/frontend

npm run dev
```

**預期輸出**:

```
  VITE v5.x.x  ready in xxx ms

  ➜  Local:   http://localhost:5173/
  ➜  Press h to show help
```

### 步驟 3: 驗證服務連接

```bash
# 檢查後端
curl http://localhost:8000/api/health

# 檢查前端
curl http://localhost:5173/
```

---

## 🧪 測試執行方式

### 方式 1️⃣: UI 模式 (推薦用於調試)

```bash
cd /Users/sacahan/Documents/workspace/CasualTrader/frontend

npx playwright test tests/e2e/E2E-003-risk-metrics.spec.js --ui
```

**特性**:

- ✅ 實時查看測試執行
- ✅ 步進調試
- ✅ 元素檢查
- ✅ 時間旅行 (時間軸回放)

**使用方式**:

1. 左側選擇測試場景
2. 點擊 ▶️ 播放按鈕
3. 在右側看到實時執行
4. 點擊步驟查看詳細信息

### 方式 2️⃣: 命令行模式 (快速驗證)

```bash
cd /Users/sacahan/Documents/workspace/CasualTrader/frontend

# 執行所有測試
npx playwright test tests/e2e/E2E-003-risk-metrics.spec.js

# 執行特定場景
npx playwright test tests/e2e/E2E-003-risk-metrics.spec.js -g "正常值顯示"

# 執行並顯示詳細日誌
npx playwright test tests/e2e/E2E-003-risk-metrics.spec.js --reporter=list

# 執行並在失敗時錄製視頻
npx playwright test tests/e2e/E2E-003-risk-metrics.spec.js --record-video=retain-on-failure
```

### 方式 3️⃣: 調試模式 (最詳細)

```bash
cd /Users/sacahan/Documents/workspace/CasualTrader/frontend

# 開啟調試器
npx playwright test tests/e2e/E2E-003-risk-metrics.spec.js --debug

# 或配合 VS Code 調試器
PWDEBUG=1 npx playwright test tests/e2e/E2E-003-risk-metrics.spec.js
```

---

## 📊 6 大測試場景詳解

### 場景 1️⃣: 正常值顯示

**測試流程**:

```
導航到首頁
  ↓
找到第一個 Agent 卡片
  ↓
點擊進入詳情頁面
  ↓
驗證 RiskMetricsCard 組件存在
  ↓
驗證三個指標都顯示正常數值（非 "—"）
  ↓
驗證都有顏色編碼
  ↓
驗證時間戳正確顯示
```

**驗證項目**:

- ✓ Sharpe Ratio: `X.XX` 格式
- ✓ Sortino Ratio: `X.XX` 格式
- ✓ Calmar Ratio: `X.XX` 格式
- ✓ 都不是 "—"（空值）
- ✓ 都有顏色標記（綠/黃/紅/灰）

**預計時間**: 10 分鐘
**成功條件**: 3/3 指標顯示正常值

---

### 場景 2️⃣: NULL 值處理

**測試流程**:

```
模擬 API 失敗（攔截並中止 /performance-history 請求）
  ↓
導航到 Agent 詳情頁面
  ↓
驗證 RiskMetricsCard 組件優雅降級
  ↓
驗證顯示 "—" 或錯誤消息
```

**驗證項目**:

- ✓ 組件不崩潰
- ✓ 顯示空值指示符 "—"
- ✓ 沒有紅色錯誤提示（或有溫和提示）

**預計時間**: 8 分鐘
**成功條件**: 優雅降級，無應用崩潰

---

### 場景 3️⃣: 邊界值測試

**測試流程**:

```
準備邊界值測試數據
  ↓
進入 Agent 詳情頁面
  ↓
驗證格式化邏輯（XX.XX 格式）
  ↓
檢查極端值正確處理
  - 高值: 3.50 ✓
  - 中等: 0.50 ✓
  - 低值: -2.50 ✓
```

**驗證項目**:

- ✓ 正值格式: `3.50`
- ✓ 零值格式: `0.50`
- ✓ 負值格式: `-2.50`
- ✓ 所有值都是 2 位小數

**預計時間**: 10 分鐘
**成功條件**: 所有邊界值正確格式化

---

### 場景 4️⃣: 顏色驗證

**測試流程**:

```
進入 Agent 詳情頁面
  ↓
檢查三個指標的背景顏色
  ↓
驗證顏色符合規範
  - RGB(16, 185, 129)   = 綠色 (優秀)
  - RGB(245, 158, 11)   = 黃色 (中等)
  - RGB(239, 68, 68)    = 紅色 (較差)
  - RGB(156, 163, 175)  = 灰色 (無數據)
  ↓
驗證懸停效果
```

**驗證項目**:

- ✓ 高值 → 綠色
- ✓ 中值 → 黃色
- ✓ 低值 → 紅色
- ✓ 空值 → 灰色
- ✓ 懸停時有視覺反饋

**預計時間**: 10 分鐘
**成功條件**: 4/4 色彩規範正確

---

### 場景 5️⃣: 響應式設計

**測試流程**:

```
改變視口尺寸到桌面 (1920×1080)
  ↓
驗證沒有水平滾軸
  ↓
驗證字體大小適當
  ↓
改變視口尺寸到平板 (768×1024)
  ↓
驗證響應式布局正確
  ↓
改變視口尺寸到手機 (375×667)
  ↓
驗證移動版顯示完美
```

**驗證項目**:

- ✓ 桌面: 3 列布局，無水平滾軸
- ✓ 平板: 1-2 列布局，字體 ≥ 14px
- ✓ 手機: 1 列布局，字體 ≥ 14px
- ✓ 所有設備都無水平滾軸

**預計時間**: 15 分鐘
**成功條件**: 3/3 設備顯示完美

---

### 場景 6️⃣: 效能測試

**測試流程**:

```
測量頁面加載性能
  ├─ TTFB (首字節) ✓
  ├─ DOM Content Loaded ✓
  └─ 完全加載 ✓
  ↓
進入詳情頁面並測量
  ├─ 進入耗時 < 3 秒 ✓
  ↓
測試 RiskMetricsCard 渲染性能
  ├─ First Paint ✓
  ├─ First Contentful Paint < 2 秒 ✓
  ↓
測試幀率
  └─ 約 60 FPS (至少 30 FPS) ✓
```

**驗證項目**:

- ✓ DOM Content Loaded < 2 秒
- ✓ 進入詳情頁面 < 3 秒
- ✓ First Contentful Paint < 2 秒
- ✓ 幀率 ≥ 30 FPS

**預計時間**: 12 分鐘
**成功條件**: 4/4 效能指標達標

---

## 📈 測試結果解讀

### 成功的測試輸出

```
✓ 場景 1: 正常值顯示 - 三個指標正確顯示數值 (10s)
✓ 場景 2: NULL 值處理 - 缺失指標顯示 "—" (8s)
✓ 場景 3: 邊界值測試 - 極端值正確處理 (10s)
✓ 場景 4: 顏色驗證 - 色彩符合規範 (10s)
✓ 場景 5: 響應式設計 - 多設備完美顯示 (15s)
✓ 場景 6: 效能測試 - 快速流暢加載 (12s)

6 passed (1m 5s)
```

### 失敗的測試輸出

```
✗ 場景 1: 正常值顯示 - 三個指標正確顯示數值
  Error: Timeout waiting for Sharpe Ratio value
  Location: line 96

✗ 場景 4: 顏色驗證 - 色彩符合規範
  Error: Expected rgb(16, 185, 129) but got rgb(156, 163, 175)
  Location: line 261

2 failed, 4 passed (1m 5s)
```

### 調試失敗

當測試失敗時，檢查以下信息：

1. **元素選擇器**

   ```
   Error: locator.click: No element matches selector '[class*="AgentCard"]'
   ```

   💡 解決: 檢查 HTML 結構，更新選擇器

2. **網路超時**

   ```
   Error: Navigation timeout of 5000ms exceeded
   ```

   💡 解決: 確認後端服務在運行，檢查網路連接

3. **顏色不符**

   ```
   Error: Expected rgb(16, 185, 129) but got rgb(156, 163, 175)
   ```

   💡 解決: 檢查 RiskMetricsCard 的色彩邏輯

4. **效能未達標**

   ```
   Error: First Contentful Paint 2500ms exceeds 2000ms limit
   ```

   💡 解決: 檢查後端 API 響應速度，考慮優化或調整閾值

---

## 🎬 生成測試報告

### HTML 報告

```bash
cd /Users/sacahan/Documents/workspace/CasualTrader/frontend

# 生成測試並創建 HTML 報告
npx playwright test tests/e2e/E2E-003-risk-metrics.spec.js --reporter=html

# 打開報告
npx playwright show-report
```

**報告包含**:

- ✓ 每個測試的執行時間
- ✓ 失敗的詳細原因
- ✓ 截圖和視頻（如有）
- ✓ 步驟詳情

### JSON 報告

```bash
# 生成 JSON 格式報告（用於自動化集成）
npx playwright test tests/e2e/E2E-003-risk-metrics.spec.js --reporter=json > test-results.json
```

### 視頻錄製

```bash
# 錄製所有測試（用空間較大）
npx playwright test tests/e2e/E2E-003-risk-metrics.spec.js --record-video=on

# 只錄製失敗的測試
npx playwright test tests/e2e/E2E-003-risk-metrics.spec.js --record-video=retain-on-failure

# 視頻位置: test-results/E2E-003-risk-metrics/...
```

---

## 🔧 常見問題排查

### Q1: "找不到 Agent 卡片"

```
ℹ️ 未找到 Agent 卡片，跳過此測試
```

**原因**:

- 後端沒有 Agent 數據
- 前端還沒加載

**解決**:

1. 確認後端服務運行
2. 檢查資料庫是否有 Agent
3. 清除瀏覽器快取

### Q2: "元素選擇器不正確"

```
Error: locator.click: Timeout waiting for element
```

**原因**:

- HTML 結構改變
- 選擇器過於嚴格

**解決**:

```bash
# 打開調試模式檢查元素
npx playwright test tests/e2e/E2E-003-risk-metrics.spec.js --debug
```

在打開的瀏覽器中:

1. 右鍵 → 檢查
2. 複製正確的 CSS 選擇器
3. 更新測試文件

### Q3: "API 響應超時"

```
Error: Navigation timeout of 5000ms exceeded
```

**原因**:

- 後端響應慢
- 網路不穩定
- API 端點錯誤

**解決**:

```bash
# 檢查後端 API
curl -v http://localhost:8000/api/trading/agents/*/performance-history

# 檢查後端日誌
# 確認 uvicorn 沒有報錯
```

### Q4: "顏色驗證失敗"

```
Error: Expected rgb(16, 185, 129) but got rgb(156, 163, 175)
```

**原因**:

- 指標的值不符合預期閾值
- 色彩對應的邏輯需要調整

**解決**:

1. 檢查 RiskMetricsCard 中的顏色邏輯
2. 驗證後端計算的值
3. 必要時調整閾值

---

## ✅ 完成檢查清單

測試完成後，請確認所有項目都已驗證：

- [ ] **場景 1 ✓**: 正常值顯示 (10 分)
  - [ ] Sharpe Ratio 顯示正常值
  - [ ] Sortino Ratio 顯示正常值
  - [ ] Calmar Ratio 顯示正常值
  - [ ] 都有顏色編碼

- [ ] **場景 2 ✓**: NULL 值處理 (8 分)
  - [ ] 組件優雅降級
  - [ ] 顯示 "—" 或錯誤提示
  - [ ] 無應用崩潰

- [ ] **場景 3 ✓**: 邊界值測試 (10 分)
  - [ ] 高值正確格式化 (3.50)
  - [ ] 中值正確格式化 (0.50)
  - [ ] 低值正確格式化 (-2.50)

- [ ] **場景 4 ✓**: 顏色驗證 (10 分)
  - [ ] 優秀 → 綠色
  - [ ] 中等 → 黃色
  - [ ] 較差 → 紅色
  - [ ] 空值 → 灰色

- [ ] **場景 5 ✓**: 響應式設計 (15 分)
  - [ ] 桌面版完美顯示 (1920×1080)
  - [ ] 平板版完美顯示 (768×1024)
  - [ ] 手機版完美顯示 (375×667)

- [ ] **場景 6 ✓**: 效能測試 (12 分)
  - [ ] DOM Content Loaded < 2 秒
  - [ ] 進入詳情頁面 < 3 秒
  - [ ] First Contentful Paint < 2 秒
  - [ ] 幀率 ≥ 30 FPS

---

## 📝 測試記錄表

將測試結果填入下表：

| 場景 | 開始時間 | 結束時間 | 耗時 | 結果 | 備註 |
|------|--------|--------|-----|------|------|
| 1️⃣ 正常值顯示 | 14:30 | 14:40 | 10分 | ✅ | - |
| 2️⃣ NULL 值處理 | 14:40 | 14:48 | 8分 | ✅ | - |
| 3️⃣ 邊界值測試 | 14:48 | 14:58 | 10分 | ✅ | - |
| 4️⃣ 顏色驗證 | 14:58 | 15:08 | 10分 | ✅ | - |
| 5️⃣ 響應式設計 | 15:08 | 15:23 | 15分 | ✅ | - |
| 6️⃣ 效能測試 | 15:23 | 15:35 | 12分 | ✅ | - |
| | | **總耗時** | **65分** | **6/6 ✅** | |

---

## 🎉 下一步

✅ **如果所有 6 個場景都通過**:

1. 更新 todo 列表，將 Phase 3.2.4 標記為已完成
2. 開始 Phase 3.3 - 性能儀表板優化
3. 參考: `docs/PHASE_3_3_DASHBOARD_PLAN.md`

❌ **如果有場景失敗**:

1. 查看失敗詳情和錯誤日誌
2. 修復對應的代碼或測試
3. 重新執行失敗的測試
4. 確認原因已解決

---

## 📚 相關資源

- **Playwright 官方文檔**: <https://playwright.dev>
- **測試文件**: `frontend/tests/e2e/E2E-003-risk-metrics.spec.js`
- **計劃文件**: `docs/PHASE_3_2_4_E2E_TEST_PLAN.md`
- **快速指南**: `docs/PHASE_3_2_4_QUICK_START.md`
- **主計劃**: `docs/PERFORMANCE_REFACTOR_CONSOLIDATED.md`

---

**祝測試順利！🚀**
